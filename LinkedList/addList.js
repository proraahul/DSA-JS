class Node{

    constructor(element){
        this.element = element;
        this.next = null;
    }
}

// LinkedList Class
class LinkedList {
    constructor(){
        this.head = null;
        this.size = null;
    }

    //! add element at the end if the list:
    add(element){
        // create a new node
        let node = new Node(element);
        
        // to store current node
        let current;

        // if list  is empty add the 
    }
}